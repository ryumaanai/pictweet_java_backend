name: CD Pipeline

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Render
        run: curl -X POST --fail "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
